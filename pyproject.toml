[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "auto-reliability"
version = "0.1.0"
description = "Leakage-safe NHTSA recall propensity proxy and Streamlit dashboard"
requires-python = ">=3.10"
dependencies = [
    "defusedxml>=0.7.1",
    "openpyxl>=3.1.5",
    "filelock>=3.15",
    "joblib>=1.3",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.1",
    "plotly>=5.18",
    "pyarrow>=14.0",
    "pypdf>=4.0",
    "reportlab>=4.0",
    "requests>=2.31",
    "scikit-learn>=1.3",
    "seaborn>=0.13",
    "streamlit>=1.31",
    "thefuzz[speedup]>=0.22",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4"]

[project.scripts]
auto-reliability = "auto_reliability.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
# El producto recurre a un baseline o diagnóstico cuando fallan artefactos
# opcionales del modelo, Parquet o informes. Estas capturas de excepciones
# en los límites son deliberadas y están cubiertas por pruebas de integración.
ignore = ["BLE001", "S112"]
